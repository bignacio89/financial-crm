# Server
NODE_ENV=development
PORT=4000

# Frontend (CORS)
FRONTEND_URL=http://localhost:3000

# Database (PostgreSQL, use same as packages/database when running locally)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/financial_crm?schema=public"

# Auth
JWT_SECRET=your-jwt-secret-change-in-production
